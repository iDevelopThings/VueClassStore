{{imports}}

export const VueClassStoresPlugin = {
	install : (app, options) => {
{{definitions}}
	}
};

import {ref} from "vue";
export const observableObject = <T>(object) => {
	return ref<T>(object);
}
