{{imports}}

export const VueClassStoresPlugin = {
	install : (Vue, options) => {
{{definitions}}
	}
};

import Vue from "vue";
export const observableObject = <T>(object) => {
	return Vue.observable<T>({} as T)
}
