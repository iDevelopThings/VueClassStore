{{imports}}

export const VueClassStoresPlugin = {
	install : (Vue, options) => {
{{definitions}}
	}
};

import Vue from "vue";
export const observableObject = <T>(obj : T) => {
	return Vue.observable<T>(obj as T)
}
