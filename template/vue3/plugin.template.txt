{{imports}}

export const VueClassStoresPlugin = {
	install : (app, options) => {
{{definitions}}
	}
};

import {ref} from "vue";
export const observableObject = <T>(obj : T) => {
	return ref<T>(obj as T);
}
